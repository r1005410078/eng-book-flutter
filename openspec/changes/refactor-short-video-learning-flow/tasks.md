## 1. Home Flow
- [x] 1.1 首页改为小视频风格学习入口（短路径进入最近学习内容）。
- [x] 1.2 保留课程切换入口，但不阻断“继续学习”主路径。
- [x] 1.3 进入首页时自动恢复上次课程与句子位置（若存在）。

## 2. Gesture Interaction
- [x] 2.1 实现左右滑切句（左滑下一句、右滑上一句）。
- [x] 2.1a 实现上下滑切单元（上滑下一单元、下滑上一单元）。
- [x] 2.2 实现长按切换模式（听力/跟读）并配置触发阈值（如 350ms）。
- [x] 2.3 跟读模式支持按住开始、松手结束的交互闭环。

## 3. Focused UI
- [x] 3.1 练习页默认仅展示核心信息（视频 + 当前句 + 可选翻译）。
- [x] 3.2 语法/用法改为抽屉按需查看，默认收起。
- [x] 3.3 保持“翻译全局开关”在切句过程中持续生效。

## 4. Playback & State
- [ ] 4.1 切句时保持媒体与时间轴同步（含跨 lesson 切媒体）。
- [ ] 4.2 切页/切模式时稳定控制播放状态，避免重复初始化与卡顿。
- [ ] 4.3 错误场景（媒体不可读、字幕缺失）提供可见降级提示。

## 5. Verification
- [ ] 5.1 补充手势行为测试（左右滑、长按触发）。
- [ ] 5.2 补充首页进入学习路径的 widget 测试。
- [ ] 5.3 补充切句同步与模式切换的回归测试。
