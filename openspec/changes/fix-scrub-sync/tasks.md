# Tasks

- [x] 在 `mock_data.dart` 中，将 `mockSentences` 改为动态生成的大列表，覆盖 0 到 600 秒。可以使用循环复制现有数据的模式。 <!-- id: 0 -->
- [x] 验证 `SentencePracticeScreen` 中的 `_syncSentenceWithVideo` 逻辑，确保 `setState` 被正确调用。 <!-- id: 1 -->
