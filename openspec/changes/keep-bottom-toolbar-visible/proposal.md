# 提案：保持底部工具栏常驻显示

## 变更背景

用户反馈底部工具栏（包含上一句、下一句、录音等核心操作）不应随视频播放控件自动隐藏，应始终保持可见以便用户随时进行操作。

## 目标

1.  **修改显隐逻辑**: 将底部的 `PracticeControls` 从自动隐藏的控制逻辑中移除，使其始终可见。

## 范围

- `SentencePracticeScreen` 中的 `build` 方法（`bottomNavigationBar` 部分）。

## 风险

- 无。
