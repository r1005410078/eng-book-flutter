# 提案：保持顶部标题栏常驻显示

## 变更背景

用户反馈在视频播放控制层自动隐藏时，顶部的标题栏（包含关闭按钮和连击计数）不应随之隐藏，而应保持常驻显示。

## 目标

1.  **修改显隐逻辑**: 将 `SentenceHeader` 从自动隐藏的控制逻辑中移除，使其始终可见。

## 范围

- `SentencePracticeScreen` 中的 `build` 方法。

## 风险

- 无。
