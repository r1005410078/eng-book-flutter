# 规格：顶部标题栏可见性

## MODIFIED Requirements

### Requirement: 标题栏常驻显示

顶部的标题栏区域（包含导航返回/关闭按钮、进度指示器、状态信息）必须 (MUST) 始终保持可见，不受视频播放控制层自动隐藏逻辑的影响。

#### Scenario: 自动隐藏触发时

- **Given** 视频正在播放且无用户操作超过 3 秒
- **When** 视频区域的控制按钮和底部控制栏自动淡出隐藏时
- **Then** 顶部的标题栏 (`_buildHeader`) 应保持完全不透明 (Opacity 1.0) 并可交互
