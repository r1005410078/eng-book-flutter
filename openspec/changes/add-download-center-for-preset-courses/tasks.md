## 1. Remove Mock Source & Empty State
- [x] 1.1 移除首页/课程选择对本地视频 mock 课程与 mock 播放数据的默认依赖。
- [x] 1.2 本地无可学习课程时，首页展示空状态引导页（文案、视觉、主按钮）。
- [x] 1.3 点击“前往下载中心”后，必须可直达下载中心页面并完成返回首页路径。

## 2. Data Contracts
- [x] 2.1 定义下载中心课程目录协议（`catalog.json` 字段：id/title/tags/size/version/url/hash/cover）。
- [x] 2.2 定义本地安装元数据（installed/downloading/failed/paused 状态）。
- [x] 2.3 定义下载目录与安装目录规范（tmp + courses）。

## 3. Download Pipeline (MVP)
- [x] 3.1 实现课程目录拉取与本地缓存。
- [x] 3.2 实现下载任务管理（开始/暂停/继续/取消/重试）。
- [x] 3.3 实现下载完成后的 hash 校验、解压安装、失败回滚。
- [x] 3.4 将下载课程接入现有本地课程枚举入口（与手动导入课程合并）。

## 4. Download Center UI
- [x] 4.1 实现下载中心页面（分类筛选、列表状态、进度条、动作按钮）。
- [x] 4.2 实现每条课程左滑删除（下载中删除临时文件、已安装删除安装目录）。
- [x] 4.3 删除当前正在学习课程时的保护策略（提示并切换或禁止）。

## 5. Reliability & Recovery
- [x] 5.1 增加空间预检（可用空间不足时禁止下载并提示）。
- [x] 5.2 App 重启后恢复下载任务与安装状态（至少恢复状态显示）。
- [x] 5.3 下载/安装失败提供可见错误状态与重试入口。

## 6. Verification
- [x] 6.1 单元测试：目录解析、状态机、hash 校验、安装回滚。
- [x] 6.2 组件测试：下载中心列表状态切换、左滑删除交互。
- [x] 6.3 回归测试：无课程时显示引导；安装课程后恢复正常学习首页；删除到空后再次显示引导。
- [x] 6.4 回归测试：下载后进入首页可直接学习；删除后课程不可再加载。
