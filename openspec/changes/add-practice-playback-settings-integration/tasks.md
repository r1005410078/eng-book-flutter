## 1. Contract And Scope
- [x] 1.1 统一定义播放设置数据模型与默认值（语速、字幕显示、译文模糊、循环次数、自动录音、字幕大小）。
- [x] 1.2 明确设置作用域（全局）与恢复策略（启动练习优先恢复最近有效设置）。

## 2. Integration
- [x] 2.1 将设置面板改为读写统一状态源，移除孤立本地状态分叉。
- [x] 2.2 在练习播放页接入设置监听，确保语速、字幕显示、字号即时生效。
- [x] 2.3 将循环次数、自动录音接入同一播放状态机，按统一顺序执行。

## 3. Fallback And Safety
- [x] 3.1 增加录音权限与平台不支持场景的降级处理（禁用或提示，不中断播放）。
- [x] 3.2 增加异常兜底：读取失败时回退默认设置且不影响练习主流程。

## 4. Verification
- [x] 4.1 新增/更新测试：设置变更后当前会话即时生效。
- [x] 4.2 新增/更新测试：退出重进后设置可恢复。
- [x] 4.3 新增/更新测试：循环与自动录音同时开启时按预期顺序执行。
- [x] 4.4 新增/更新测试：录音权限缺失或平台不支持时正确降级。
