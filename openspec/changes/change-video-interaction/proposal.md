# 提案：视频点击交互变更为播放/暂停

## 变更背景

用户反馈目前的视频点击交互（显示/隐藏控制层）不符合其预期。用户希望点击视频区域（即使控制层已隐藏）能直接触发播放或暂停操作。

## 目标

1.  **修改交互逻辑**: 视频区域的点击事件由“切换控制层显隐”改为“切换播放/暂停状态”。

## 范围

- `SentencePracticeScreen` 中的 `_buildVideoArea` 方法中的 `GestureDetector`。

## 风险

- 用户可能无法仅显示控制层而不改变播放状态（通常点击暂停后控制层会自动显示，所以应该是可接受的）。
