# Tasks

- [x] 在 `SentencePracticeScreen` 中添加 `_controlsVisible` 状态和自动隐藏计时器逻辑。 <!-- id: 0 -->
- [x] 将顶部 `Header`、中间 `PlayButton`、底部 `PracticeControls` 包裹在 `AnimatedOpacity` 中，受 `_controlsVisible` 控制。 <!-- id: 1 -->
- [x] 实现点击视频区域 Toggle `_controlsVisible` 的交互。 <!-- id: 2 -->
- [x] 在视频底部添加 `VideoProgressIndicator` 或自定义进度条，同样受 `_controlsVisible` 控制。 <!-- id: 3 -->
- [x] 确保在 3 秒无操作后自动触发隐藏（仅在播放状态下）。 <!-- id: 4 -->
